-- Fix description
version: 2

models:
    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique

    - name: my_second_dbt_model
      description: "A starter dbt model with a ref function"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: long_sql_model_file_name_this_model_has_many_columns_n_rows_to_test_cols_n_rows_so_lets_add_a_long_name_to_test_that_too
      description: "{{ doc('long_col_names')}}"